# 题目：数字比较。

if __name__ == '__main__':
    a = 30
    b = 20
    if a > b:
        print("%d > %d" % (a, b))
    elif a < b:
        print("%d < %d" % (a, b))
    else:
        print("%d = %d" % (a, b))

